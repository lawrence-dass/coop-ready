# SubmitSmart Environment Variables Template
# Copy this file to .env.local and fill in your actual values
# DO NOT commit .env.local to version control!

# ==============================================================================
# SUPABASE CONFIGURATION
# ==============================================================================
# Get these from your Supabase project: https://supabase.com/dashboard/project/_/settings/api

# Supabase Project URL (publicly safe to expose)
# Location: Supabase Dashboard → Settings → API → Project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase Anonymous Key (publicly safe for client-side auth)
# Location: Supabase Dashboard → Settings → API → Project API keys → anon/public
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase Service Role Key (SERVER-SIDE ONLY - NEVER expose to browser!)
# Location: Supabase Dashboard → Settings → API → Project API keys → service_role
# Used for: Admin operations, bypassing RLS when needed
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ==============================================================================
# ANTHROPIC (CLAUDE) API CONFIGURATION
# ==============================================================================
# Get your API key from: https://console.anthropic.com/settings/keys

# Anthropic API Key (SERVER-SIDE ONLY - NEVER expose to browser!)
# Used for: LLM-powered resume optimization suggestions
# Cost: ~$0.10 per optimization (60s timeout max)
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# LLM Model Tier (SERVER-SIDE ONLY)
# Controls which Claude models are used for suggestion generation
# Options:
#   - development: Uses Haiku for everything (faster, cheaper ~$0.01-0.02)
#   - production: Uses Sonnet for suggestions, Haiku for judge (better quality ~$0.08-0.12)
# Default: development (cost-safe default if not set)
LLM_TIER=development

# ==============================================================================
# IMPORTANT SECURITY NOTES
# ==============================================================================
# 1. Variables prefixed with NEXT_PUBLIC_ are exposed to the browser
# 2. Server-only secrets (API keys) MUST NOT use NEXT_PUBLIC_ prefix
# 3. Never commit .env.local to git - it's already in .gitignore
# 4. Rotate keys immediately if accidentally exposed
